// SiteHeader is no longer used as standalone chrome.
// Auth + shell structure live in ThemeShell (see app/layout.tsx).
// Kept as a thin re-export for any legacy imports.
export { ThemeShell as SiteHeader } from '@/components/theme/theme-shell';
